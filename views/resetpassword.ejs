<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>

  <body>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      .wrapper {
        display: flex;
        flex-direction: column;
        max-width: 500px;
      }
      .inputBox {
        font-size: 1.3rem;
        padding: 12px;
        margin: 8px;
        border: 1px solid rgba(0, 0, 0, 0.288);
        border-radius: 8px;
      }
      .wrapper button {
        max-width: 100px;
        padding: 6px;
        margin: 8px;
        border: 1px solid rgba(0, 0, 0, 0.288);
        border-radius: 5px;
      }
    </style>
    <form action="/api/v1/user/reset-password?token=<%= token %>" method="post">
      <div class="wrapper">
        <input
          type="password"
          class="inputBox"
          placeholder="New Password"
          name="newPassword"
        />
        <input
          type="password"
          class="inputBox"
          placeholder="Confirm Password"
          name="confirmPassword"
        />
        <button type="submit">Submit</button>
      </div>
    </form>
  </body>
</html>
